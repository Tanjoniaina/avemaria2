<form method="post" action="{{ path('app_pharmaciegros_entity_purchaseorderline_delete', {'id': purchaseorderline.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ purchaseorderline.id) }}">
    <button class="btn">Delete</button>
</form>
