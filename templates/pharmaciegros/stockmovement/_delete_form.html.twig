<form method="post" action="{{ path('app_pharmaciegros_entity_stockmovement_delete', {'id': stockmovement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stockmovement.id) }}">
    <button class="btn">Delete</button>
</form>
