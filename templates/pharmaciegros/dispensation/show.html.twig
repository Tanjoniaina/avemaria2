{% extends 'base.html.twig' %}

{% block title %}Dispensation{% endblock %}

{% block body %}
    <h1>Dispensation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ dispensation.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ dispensation.date ? dispensation.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ dispensation.quantity }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ dispensation.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmaciegros_entity_dispensation_index') }}">back to list</a>

    <a href="{{ path('app_pharmaciegros_entity_dispensation_edit', {'id': dispensation.id}) }}">edit</a>

    {{ include('pharmaciegros/dispensation/_delete_form.html.twig') }}
{% endblock %}
