{% extends 'base.html.twig' %}

{% block title %}Category{% endblock %}

{% block body %}
    <h1>Category</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ category.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmaciegros_entity_category_index') }}">back to list</a>

    <a href="{{ path('app_pharmaciegros_entity_category_edit', {'id': category.id}) }}">edit</a>

    {{ include('app/pharmaciegros/entity/category/_delete_form.html.twig') }}
{% endblock %}
