{% extends '../../base.html.twig' %}

{% block title %}Payment{% endblock %}

{% block body %}
    <h1>Payment</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ payment.id }}</td>
            </tr>
            <tr>
                <th>Payementdate</th>
                <td>{{ payment.payementdate ? payment.payementdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ payment.amount }}</td>
            </tr>
            <tr>
                <th>Payementmethod</th>
                <td>{{ payment.payementmethod }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ payment.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmaciegros_entity_payment_index') }}">back to list</a>

    <a href="{{ path('app_pharmaciegros_entity_payment_edit', {'id': payment.id}) }}">edit</a>

    {{ include('app/pharmaciegros/entity/payment/_delete_form.html.twig') }}
{% endblock %}
