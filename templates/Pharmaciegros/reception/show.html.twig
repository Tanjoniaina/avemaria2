{% extends '../../base.html.twig' %}

{% block title %}Reception{% endblock %}

{% block body %}
    <h1>Reception</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reception.id }}</td>
            </tr>
            <tr>
                <th>Receiveddate</th>
                <td>{{ reception.receiveddate ? reception.receiveddate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ reception.status }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmaciegros_entity_reception_index') }}">back to list</a>

    <a href="{{ path('app_pharmaciegros_entity_reception_edit', {'id': reception.id}) }}">edit</a>

    {{ include('app/pharmaciegros/entity/reception/_delete_form.html.twig') }}
{% endblock %}
