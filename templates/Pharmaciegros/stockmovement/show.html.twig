{% extends '../../base.html.twig' %}

{% block title %}Stockmovement{% endblock %}

{% block body %}
    <h1>Stockmovement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stockmovement.id }}</td>
            </tr>
            <tr>
                <th>Movementdate</th>
                <td>{{ stockmovement.movementdate ? stockmovement.movementdate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ stockmovement.quantity }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ stockmovement.type }}</td>
            </tr>
            <tr>
                <th>Comment</th>
                <td>{{ stockmovement.comment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmaciegros_entity_stockmovement_index') }}">back to list</a>

    <a href="{{ path('app_pharmaciegros_entity_stockmovement_edit', {'id': stockmovement.id}) }}">edit</a>

    {{ include('app/pharmaciegros/entity/stockmovement/_delete_form.html.twig') }}
{% endblock %}
