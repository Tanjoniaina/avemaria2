{% extends 'base.html.twig' %}

{% block title %}Location{% endblock %}

{% block body %}
    <h1>Location</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ location.name }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ location.code }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ location.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pharmaciegros_entity_location_index') }}">back to list</a>

    <a href="{{ path('app_pharmaciegros_entity_location_edit', {'id': location.id}) }}">edit</a>

    {{ include('pharmaciegros/location/_delete_form.html.twig') }}
{% endblock %}
