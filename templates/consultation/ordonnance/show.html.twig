{% extends 'base.html.twig' %}

{% block title %}Ordonnance{% endblock %}

{% block body %}
    <h1>Ordonnance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ordonnance.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ ordonnance.numero }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ ordonnance.date ? ordonnance.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ ordonnance.quantity }}</td>
            </tr>
            <tr>
                <th>Posologie</th>
                <td>{{ ordonnance.posologie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_consultation_entity_ordonnance_index') }}">back to list</a>

    <a href="{{ path('app_consultation_entity_ordonnance_edit', {'id': ordonnance.id}) }}">edit</a>

    {{ include('consultation/ordonnance/_delete_form.html.twig') }}
{% endblock %}
