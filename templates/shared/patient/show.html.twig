{% extends 'base.html.twig' %}

{% block title %}Patient{% endblock %}

{% block body %}
    <h1>Patient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ patient.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ patient.numero }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ patient.nom }}</td>
            </tr>
            <tr>
                <th>Prenoms</th>
                <td>{{ patient.prenoms }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ patient.telephone }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ patient.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_shared_entity_patient_index') }}">back to list</a>

    <a href="{{ path('app_shared_entity_patient_edit', {'id': patient.id}) }}">edit</a>

    {{ include('Shared/patient/_delete_form.html.twig') }}
{% endblock %}
