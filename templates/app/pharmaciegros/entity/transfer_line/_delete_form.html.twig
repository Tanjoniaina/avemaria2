<form method="post" action="{{ path('app_pharmaciegros_entity_transfer_line_delete', {'id': transfer_line.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfer_line.id) }}">
    <button class="btn">Delete</button>
</form>
