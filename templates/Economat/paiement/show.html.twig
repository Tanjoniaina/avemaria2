{% extends 'base.html.twig' %}

{% block title %}Paiement{% endblock %}

{% block body %}
    <h1>Paiement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ paiement.id }}</td>
            </tr>
            <tr>
                <th>Datedepaiement</th>
                <td>{{ paiement.datedepaiement ? paiement.datedepaiement|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ paiement.montant }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_economat_entity_paiement_index') }}">back to list</a>

    <a href="{{ path('app_economat_entity_paiement_edit', {'id': paiement.id}) }}">edit</a>

    {{ include('economat/paiement/_delete_form.html.twig') }}
{% endblock %}
