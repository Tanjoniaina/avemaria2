{% extends 'base.html.twig' %}

{% block title %}Ordonnance index{% endblock %}

{% block body %}
    <h1>Ordonnance index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>Date</th>
                <th>Quantity</th>
                <th>Posologie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for ordonnance in ordonnances %}
            <tr>
                <td>{{ ordonnance.id }}</td>
                <td>{{ ordonnance.numero }}</td>
                <td>{{ ordonnance.date ? ordonnance.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ ordonnance.quantity }}</td>
                <td>{{ ordonnance.posologie }}</td>
                <td>
                    <a href="{{ path('app_consultation_entity_ordonnance_show', {'id': ordonnance.id}) }}">show</a>
                    <a href="{{ path('app_consultation_entity_ordonnance_edit', {'id': ordonnance.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="6">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_consultation_entity_ordonnance_new') }}">Create new</a>
{% endblock %}
