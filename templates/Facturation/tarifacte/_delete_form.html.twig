<form method="post" action="{{ path('app_shared_entity_tarifacte_delete', {'id': tarifacte.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarifacte.id) }}">
    <button class="btn">Delete</button>
</form>
