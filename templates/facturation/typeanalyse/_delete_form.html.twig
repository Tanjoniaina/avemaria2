<form method="post" action="{{ path('app_facturation_entity_typeanalyse_delete', {'id': typeanalyse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ typeanalyse.id) }}">
    <button class="btn">Delete</button>
</form>
